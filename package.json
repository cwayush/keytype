{
  "name": "keytype",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build --filter=frontend"
  },
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
