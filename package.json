{
  "name": "keytype",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "frontend",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^2.5.6"
  },
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  }
}
