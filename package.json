{
  "name": "keytype",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=frontend",
    "postinstall": "cd packages/db && npx prisma generate",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "db:setup": "cd packages/db && npm run db:setup",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "start": "turbo run start --filter=frontend"

  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.5.6",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "frontend",
    "packages/*"
  ]
  
}
