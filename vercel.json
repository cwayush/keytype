{
  "buildCommand": "npm install --workspaces=false && npx turbo run generate --filter=@repo/db && npx turbo run build --filter=frontend",
  "rootDirectory": "frontend",
  "includeFiles": ["../packages/db/**"]
}
